[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "irec"
version = "1.0.0"
description = "Framework for R&D RecSys projects"
authors = [
    {name = "Vladimir Baikalov", email = "nonameuntitled159@gmail.com"}
]
requires-python = ">=3.12"